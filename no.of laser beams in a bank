class Solution:
    def numberOfBeams(self, bank: List[str]) -> int:
        prev=0
        beams=0
        for row in bank :
            cnt=row.count('1')
            if cnt:
                beams+=prev*cnt
                prev=cnt
        return beams
