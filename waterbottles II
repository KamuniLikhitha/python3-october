class Solution:
    def maxBottlesDrunk(self, numBottles: int, numExchange: int) -> int:
        ans=numBottles
        empties=numBottles
        k=numExchange
        while empties>=k:
            empties=empties-k+1
            ans+=1
            k+=1
        return ans
