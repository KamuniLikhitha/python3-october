class Solution:
    def maximumEnergy(self, energy: List[int], k: int) -> int:
        n=len(energy)
        dp=energy[:]
        for i in range(n-1,-1,-1):
            j=i+k
            if j<n:
                dp[i]+=dp[j]
        return max(dp)
